{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "proof-window"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
