# Core Cards

Use for cards whose primary write scope is compiler, VM, runtime, tbackend, or
forms/core execution surfaces.

Required header:

```text
Category: core
```

Durable docs go to `lab-docs/core/`.

