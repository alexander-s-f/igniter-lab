# View Cards

Use for cards whose primary write scope is the View DSL, view artifacts,
safe rendering, hot reload preview, and isomorphic view framework experiments.

Required header:

```text
Category: view
```

Durable docs go to `lab-docs/view/`.

