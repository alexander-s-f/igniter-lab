# Stdlib Cards

Use for cards whose primary write scope is stdlib candidates, IO/effect
surfaces, capability passports, app fixtures, or utility applications.

Required header:

```text
Category: stdlib
```

Durable docs go to `lab-docs/stdlib/`.

