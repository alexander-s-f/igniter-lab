# GUI Cards

Use for cards whose primary write scope is native GUI engine, scene/layout
systems, design-system experiments, machine UI mechanics, or Tailmix-inspired
GUI interaction work.

Required header:

```text
Category: gui
```

Durable docs go to `lab-docs/gui/`.

