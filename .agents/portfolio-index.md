# igniter-lab: Portfolio Index
LAB-STDLIB-IS-EMPTY-P1 CLOSED (READINESS PROOF — 48/48 PASS; ACCEPT; stdlib.collection.is_empty + stdlib.collection.non_empty both ready for proposal; BOTH FUNCTIONS REQUIRED: non_empty cannot be derived — ! unary_op parsed but NOT type-checked (infer_expr has no when "unary_op" arm → OOF-TY0 "Unsupported expression kind: unary_op"); is_empty : Collection[T] → Bool; non_empty : Collection[T] → Bool; pure/total/authority_surface:none; NO new OOF codes: OOF-COL1 arity + OOF-COL2 non-Collection reused from count; APP PRESSURE: state_machine.ig explicitly documents gap ("we lack is_empty()") + bloom_filter/ops.ig non-empty check need (3 fixtures / 5 comments); DISPATCH SHAPE: 1-arg no-lambda Collection→Bool — direct when "is_empty","non_empty" arm + infer_is_empty_call (matches fold/sum precedent); SIR fn = qualified canonical names; OOF-COL6 available but deferred; head()/find_one() are separate future cards; lab doc: igniter-lab/lab-docs/governance/lab-stdlib-is-empty-readiness-v0.md; proof: igniter-lab/igniter-view-engine/proofs/verify_lab_stdlib_is_empty_p1.rb; next LANG-STDLIB-IS-EMPTY-PROP-P1 proposal authoring)
