# lab-igniter-lab-generated-output-hygiene-p2-v0 — generated output hygiene map

**Card:** `LAB-IGNITER-LAB-GENERATED-OUTPUT-HYGIENE-P2` · **Delegation:** `OPUS-LAB-GENERATED-HYGIENE-A`
**Status:** READINESS / CLEANUP MAP (no deletion). Inventories generated/noise surfaces and the smallest
safe cleanup. **No `rm`/`git rm`/`mv`/copy/archive/dir-create, no `.gitignore`/Cargo/code change, no
symlink change.**
**Authority:** Lab hygiene map. Verified against the live tree + git 2026-06-19.

## 1. Executive summary

- The lab's generated/local state is **already well-ignored**: `target/`, `**/out/`, `node_modules`,
  `build`, `.svelte-kit`, `.idea`, `.codex-local` all return **IGNORED** from `git check-ignore`. 76
  ignored entries; tracked tree is clean (only this card untracked). So generated weight is **not**
  inflating the tracked repo — it's local disk only.
- **Two corrections to prior maps:**
  1. **`ln` is a phantom coupling.** P1/P2 said "machine tests read `../ln/...`". Live truth: machine
     tests locate fixtures via `concat!(env!("CARGO_MANIFEST_DIR"), "/../igniter-apps/web_router")`
     (`igniter-machine/tests/machine_tests.rs:182`). `rg "ln/|/ln|\"ln\""` over `igniter-machine/{tests,
     src}` = **empty**; `igniter-lab/ln` is **absent**. There is **no live `../ln` test dependency** —
     retire that concern.
  2. `igniter-view-engine/out/` is **IGNORED but LOAD_BEARING** (IDE reads it at runtime) — confirmed, do
     not clean.
- **Exactly one genuinely safe later cleanup:** the empty, ignored `igniter-compiler/igniter-view-engine/`
  stub (`out/p12`, 0 files). Everything else is either load-bearing, regenerable-local (leave), or a
  **tracked** stale stub needing a deliberate `git rm` (`igniter-lab/igniter-lab/`, `igniter-site/`).

## 2. Inventory by generated/noise class

| Class | Count / paths | Tracked? |
|---|---|---|
| **CARGO_TARGET** | 11 — `igniter-{3d,compiler,console,frame,gui,machine,server,stdlib,tbackend,ui-kit,vm,web}/target`, `igniter-ide/src-tauri/target` | IGNORED |
| **NODE_BUILD** | `igniter-ide/{node_modules,.svelte-kit,build}`, `igniter-jetbrains-plugin/build` | IGNORED |
| **GENERATED_OUT** | `**/out/` — `igniter-{3d-poc,gui-engine,vm,stdlib,compiler}/out`, `igniter-research/{igniter-stdlib,ivm-ruby-runtime}/out`, `igniter-compiler/igniter-view-engine/out` | IGNORED (`.gitignore:16 **/out/`) |
| **LOAD_BEARING_GENERATED** | `igniter-view-engine/out/` (IDE runtime input) | IGNORED but **keep** |
| **IDE_LOCAL_STATE** | 7 `.idea` (root, compiler, machine, stdlib, tbackend, vm, jetbrains-plugin), `.codex-local` | IGNORED |
| **EMPTY_STUB** | `igniter-compiler/igniter-view-engine/` (only empty `out/p12`, 0 files) | untracked/ignored |
| **STALE_SITE / STALE_STUB (tracked)** | `igniter-site/` (2 files: README+.gitignore), nested `igniter-lab/igniter-lab/` (2 tracked files under `.agents/`) | **TRACKED** |
| **SYMLINK_OR_LINK** | none (`find -type l` empty); `igniter-lab/ln` **ABSENT** | — |
| **PROOF_OUTPUT** | the various `*/out/` under historical proof trees (3d-poc, gui-engine, research, vm, stdlib) — regenerable proof artifacts | IGNORED |

## 3. Tracked vs ignored table (cleanup candidates)

| Path | Status | Class |
|---|---|---|
| all `target/` (×12) | IGNORED, regenerated by Cargo | KEEP (not this card) |
| `igniter-ide/{node_modules,.svelte-kit,build}` | IGNORED, local build state | KEEP (not this card) |
| `**/out/` proof outputs | IGNORED | KEEP_EVIDENCE (regenerable) |
| `igniter-view-engine/out/` | IGNORED | **KEEP_LOAD_BEARING** |
| `.idea` (×7), `.codex-local` | IGNORED, local | UNTRACKED_LOCAL (not this card) |
| `igniter-compiler/igniter-view-engine/` | ignored empty (`**/out/`), 0 files, no referrers | **SAFE_DELETE_CANDIDATE** |
| `igniter-site/` | **TRACKED** (2 files) | NEEDS_HUMAN_DECISION (deliberate `git rm`) |
| nested `igniter-lab/igniter-lab/` | **TRACKED** (2 files) | NEEDS_HUMAN_DECISION (deliberate `git rm`) |

(Classifications from `git ls-files` / `git status --ignored` / `git check-ignore`, not from names.)

## 4. Load-bearing generated outputs

- **`igniter-view-engine/out/` → `KEEP_LOAD_BEARING`.** Gitignored, but `igniter-ide` reads it at runtime
  (21 refs, P2/P3). Marked in `igniter-view-engine/STATUS.md`. Do **not** clean until the IDE
  path/runtime expectation is refactored.
- All other `out/`/`target/` are **regenerable** (Cargo / proof runners). Disposable in principle, but
  **not in any cleanup card** — deleting `target/` only forces a rebuild, no hygiene win worth the churn.

## 5. `ln` reference / absence analysis

- **`igniter-lab/ln`: ABSENT** (`test -e ln` → absent; no symlink at `find -type l`).
- **No live test/src references it.** Machine tests use `../igniter-apps/web_router`
  (`machine_tests.rs:182`); `rg "ln/|/ln|\"ln\""` over `igniter-machine/{tests,src}` = empty.
- The only `../ln`-ish hits are in `igniter-design-system/*.html` (a spurious substring match in design
  docs) and in the P1/P2 lab-docs themselves (where the inaccurate claim originated).
- **Conclusion:** the "`../ln` machine-test coupling" is a **phantom** — there is nothing to restore,
  refactor, or own. No `LN-FIXTURE-ROOT` card is needed; instead P1's memory/claim is corrected here.

## 6. Cleanup candidate table

| Path | Evidence | Status | Consumers | Risk | Label | Owner card |
|---|---|---|---|---|---|---|
| `igniter-compiler/igniter-view-engine/` | 0 files, empty `out/p12`, ignored, no referrers (IDE uses lab-root copy, P2) | ignored/empty | none | very low | **SAFE_DELETE_CANDIDATE** | `…GENERATED-OUTPUT-CLEANUP-P3` |
| nested `igniter-lab/igniter-lab/` | 2 tracked files under `.agents/`, near-empty | TRACKED | unknown | low-med | **NEEDS_HUMAN_DECISION** | `…STALE-STUB-DISPOSITION-P3` |
| `igniter-site/` | 2 tracked files (README+.gitignore), no active consumer found | TRACKED | none found | low | **ARCHIVE_CANDIDATE / NEEDS_HUMAN** | `…STALE-STUB-DISPOSITION-P3` |
| `igniter-view-engine/out/` | IDE runtime input | ignored | igniter-ide | — | **KEEP_LOAD_BEARING** | — |
| all `target/`, other `out/`, node_modules, .idea, .codex-local | regenerable / local | ignored | build tools | — | KEEP / not-this-card | — |

## 7. Protected paths / non-actions

- `igniter-view-engine/out/`, `fixtures/`, `igniter_view_runtime.js`, proof runners — KEEP (IDE).
- All `target/` — regenerated by Cargo; do not delete (no win, forces rebuilds).
- `**/out/` proof trees (3d-poc, gui-engine, research, vm, stdlib) — KEEP_EVIDENCE; regenerable, may be
  cited.
- Tracked stubs (`igniter-site/`, nested `igniter-lab/igniter-lab/`) — only via deliberate `git rm` under
  a human-gated card, not blind cleanup.
- **No** `rm`/`git rm`/`mv`/archive/dir-create/`.gitignore`/symlink change performed here.

## 8. Later `.gitignore` / policy suggestions (recommend only)

- Ignore coverage is **already good** (`target/`, `**/out/`, `node_modules`, `build`, `.svelte-kit`,
  `.idea`, `.codex-local` all IGNORED). No urgent gap.
- Optional later: a one-line root-`.gitignore` comment documenting that `igniter-view-engine/out/` is
  ignored-yet-IDE-load-bearing (pointer to its `STATUS.md`), so nobody "fixes" the ignore.
- Optional later: a generated-output convention note (every crate's `out/`/`target/` is ephemeral except
  the view-engine `out/`), and a proof-output retention rule (which `*/out/` proofs are evidence vs
  trash) — both doc-only, deferred.

## 9. Agent parallelization notes

- **Gemini:** broad ignored/generated inventory + path-reference census (one lab artifact; no deletion,
  no gov writes).
- **Sonnet:** critic over the delete candidates + the LOAD_BEARING exception; check the `ln`-phantom
  correction isn't itself overstated.
- **Codex:** live verifier for `git ls-files`/`check-ignore`/symlinks; executes mechanical cleanup ONLY
  under the explicit P3 cleanup cards.
- **Opus:** this synthesis + the risk-ranked candidate table.

## 10. Next cards + how this feeds repo-boundary

**Next cards (≤3; ≥1 tiny-safe, ≤1 structural):**
1. **`LAB-IGNITER-LAB-GENERATED-OUTPUT-CLEANUP-P3`** *(tiny, safe)* — delete only the empty ignored
   `igniter-compiler/igniter-view-engine/` stub. Nothing else.
2. **`LAB-IGNITER-LAB-STALE-STUB-DISPOSITION-P3`** *(no-code / human-gated)* — decide `git rm` vs keep for
   the two TRACKED stubs (`igniter-site/`, nested `igniter-lab/igniter-lab/`).
3. **`LAB-IGNITER-CARGO-WORKSPACE-ROOT-P3`** *(the one structural)* — additive Cargo workspace root (no
   dir moves); also the host for the `.ig` import-ownership workspace resolver.

**Feeds repo-boundary:** generated weight is already ignored (won't distort the split); the `ln` phantom
is retired (one fewer fake blocker); the only real pre-split hygiene is the empty compiler stub + the two
tracked stubs; the structural enabler remains the Cargo workspace root. The view-engine path refactor and
physical repo split stay gated behind that root.

---

*Hygiene map only. 2026-06-19, live tree + git (ignored visible). No deletion/move/`.gitignore`/code
change. Headline: ignores are already good; `ln` is a phantom (retire it); one safe delete = the empty
compiler view-engine stub; `igniter-view-engine/out/` stays load-bearing.*
