[package]
name = "igniter_stdlib"
version = "0.1.0"
edition = "2021"

[lib]
name = "igniter_stdlib"
crate-type = ["rlib", "cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

